# Local:
VITE_API_BASE=http://localhost:8000

# Production (set in Netlify → Site settings → Environment variables):
# VITE_API_BASE=https://YOUR-EASYPANEL-BACKEND-HOST
# No trailing slash. Rebuild after changing it (Vite inlines at build time).
