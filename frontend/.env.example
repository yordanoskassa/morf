# API is hardcoded in src/core/morphClient.ts:
# https://anton-morf.hrvnvm.easypanel.host
#
# Optional override for local experiments (ignored while BASE is hardcoded):
# VITE_API_BASE=http://localhost:8000
